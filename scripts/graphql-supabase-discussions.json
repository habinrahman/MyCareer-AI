{
  "query": "query { repository(owner: \"supabase\", name: \"supabase\") { discussions(first: 40, orderBy: {field: UPDATED_AT, direction: DESC}) { nodes { id number title url category { name } comments { totalCount } answer { url } } } } }"
}
