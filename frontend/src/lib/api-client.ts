/**
 * @deprecated Import from `@/lib/api` instead (`api`, `apiClient`, `getApiBaseUrl`).
 * Kept for stable import paths across the codebase.
 */
export {
  apiClient,
  getApiBaseUrl,
  setApiAuthTokenGetter,
  ApiError,
  apiFetch,
} from "@/lib/api";
export { default } from "@/lib/api";
